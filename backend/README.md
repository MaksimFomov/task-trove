# TaskTrove

Платформа для связи заказчиков и исполнителей, построенная на Spring Boot (backend) и React + TypeScript (frontend).

## Быстрый старт

### Требования

- Java 21+
- MySQL 8.0+
- Maven 3.8+
- Node.js 18+

### Установка и запуск

#### 1. Настройка Backend

1. **Клонируйте репозиторий** (если еще не сделали):
   ```bash
   git clone <repository-url>
   cd tasktrove
   ```

2. **Создайте файл `.env`** на основе примера:
   ```bash
   cp .env.example .env
   ```

3. **Настройте переменные окружения** в файле `.env`:
   ```properties
   # Database Configuration
   DB_URL=jdbc:mysql://localhost:3306/tasktrove
   DB_USERNAME=root
   DB_PASSWORD=your_database_password
   
   # JWT Configuration
   JWT_SECRET=your_secure_jwt_secret_minimum_32_characters_long
   
   # Email Configuration
   MAIL_USERNAME=your_email@gmail.com
   MAIL_PASSWORD=your_gmail_app_password
   ```

4. **Создайте базу данных**:
   ```sql
   CREATE DATABASE tasktrove CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

5. **Загрузите переменные окружения и запустите приложение**:
   
   **Вариант A - IntelliJ IDEA:**
   - Откройте Run → Edit Configurations
   - Добавьте Environment Variables из файла `.env`
   - Запустите приложение

   **Вариант B - Командная строка (Linux/Mac):**
   ```bash
   export $(cat .env | xargs)
   mvn spring-boot:run
   ```

   **Вариант C - Командная строка (Windows):**
   ```cmd
   # Установите переменные вручную или используйте PowerShell
   mvn spring-boot:run
   ```

Backend будет доступен по адресу: `http://localhost:8080`

#### 2. Настройка Frontend

1. **Перейдите в директорию frontend**:
   ```bash
   cd frontend
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   ```

3. **Запустите development сервер**:
   ```bash
   npm run dev
   ```

Frontend будет доступен по адресу: `http://localhost:3000`

## Структура проекта

```
tasktrove/
├── src/main/java/com/fomov/tasktroveapi/  # Backend код
│   ├── controller/                          # REST контроллеры
│   ├── service/                             # Бизнес-логика
│   ├── repository/                          # Доступ к данным
│   ├── model/                               # JPA сущности
│   ├── dto/                                 # Data Transfer Objects
│   ├── security/                            # JWT и безопасность
│   └── websocket/                           # WebSocket для чатов
├── frontend/                                # Frontend приложение
│   ├── src/
│   │   ├── pages/                           # Страницы приложения
│   │   ├── components/                      # React компоненты
│   │   ├── services/                        # API клиенты
│   │   └── types/                           # TypeScript типы
└── .env.example                             # Пример конфигурации
```

## Безопасность

### Важные замечания

- ⚠️ **Никогда не коммитьте файл `.env`** в систему контроля версий
- 🔐 Используйте **сильные пароли** для базы данных
- 🔑 JWT secret должен быть **минимум 32 символа** для алгоритма HS256
- 📧 Для Gmail используйте **App-specific passwords**, а не основной пароль:
  1. Включите двухфакторную аутентификацию в Google аккаунте
  2. Перейдите в Security → App passwords
  3. Создайте новый app password для приложения
  4. Используйте этот пароль в переменной `MAIL_PASSWORD`

### Production окружение

В production используйте системы управления секретами:
- AWS Secrets Manager
- Azure Key Vault
- HashiCorp Vault
- Kubernetes Secrets

## Основные возможности

### Для заказчиков (Customers)
- Создание и управление заказами
- Просмотр откликов от исполнителей
- Выбор исполнителя для заказа
- Отправка технического задания по email
- Чат с исполнителем в реальном времени
- Оставление отзывов после выполнения

### Для исполнителей (Performers)
- Просмотр доступных заказов
- Отправка откликов на заказы
- Управление портфолио
- Чат с заказчиком
- Отслеживание рейтинга

### Для администраторов (Administrators)
- Управление пользователями
- Модерация контента
- Активация/деактивация портфолио

## API Документация

Backend предоставляет REST API на `http://localhost:8080/api`:

### Основные endpoints

- `/api/auth` - Аутентификация и регистрация
- `/api/customers` - Функции заказчика
- `/api/performers` - Функции исполнителя
- `/api/admin` - Административные функции
- `/api/orders` - Управление заказами
- `/api/chats` - Управление чатами

WebSocket для чатов: `ws://localhost:8080/ws/chat/{chatId}`

### Формат ответов API

Все API endpoints используют стандартизированный формат ответов:

**Успешный ответ:**
```json
{
  "success": true,
  "data": { ... },
  "timestamp": "2025-11-15T11:40:47.123Z"
}
```

**Ответ с ошибкой:**
```json
{
  "success": false,
  "message": "Error message",
  "status": 400,
  "timestamp": "2025-11-15T11:40:47.123Z"
}
```

Подробная документация API:
- `API_RESPONSE_FORMAT.md` - Формат ответов API
- `API_CHANGES.md` - Список изменений API
- `PROJECT_DESCRIPTION.md` - Описание функциональности

## Технологии

### Backend
- Spring Boot 3.x
- Spring Security + JWT
- Spring Data JPA
- MySQL
- WebSocket
- JavaMail

### Frontend
- React 18
- TypeScript
- Vite
- TanStack Query
- Zustand (state management)
- Tailwind CSS

## Разработка

### Запуск тестов

Backend:
```bash
mvn test
```

Frontend:
```bash
cd frontend
npm test
```

### Сборка для production

Backend:
```bash
mvn clean package
java -jar target/tasktroveapi-0.0.1-SNAPSHOT.jar
```

Frontend:
```bash
cd frontend
npm run build
# Результат сборки будет в директории frontend/dist
```

### Качество кода

Проект следует следующим стандартам:

- **Naming conventions**: camelCase для параметров и полей
- **Logging**: Использование SLF4J logger (не System.out)
- **Transactions**: @Transactional для multi-step операций
- **Error handling**: Стандартизированные error responses
- **Security**: Environment variables для секретов
- **Architecture**: Разделение на Controller → Service → Repository слои

### Недавние улучшения

- ✅ Стандартизирован формат API ответов
- ✅ Добавлена транзакционность для критических операций
- ✅ Оптимизированы database queries (решены N+1 проблемы)
- ✅ Улучшена безопасность (environment variables)
- ✅ Очищен неиспользуемый код
- ✅ Стандартизировано логирование

См. `API_CHANGES.md` для полного списка изменений.

## Переменные окружения

### Обязательные переменные

| Переменная | Описание | Пример |
|------------|----------|--------|
| `DB_URL` | URL подключения к MySQL | `jdbc:mysql://localhost:3306/tasktrove` |
| `DB_USERNAME` | Имя пользователя БД | `root` |
| `DB_PASSWORD` | Пароль БД | `your_password` |
| `JWT_SECRET` | Секретный ключ JWT (мин. 32 символа) | `your_secret_key_32_chars_minimum` |
| `MAIL_USERNAME` | Email для отправки уведомлений | `your_email@gmail.com` |
| `MAIL_PASSWORD` | Пароль email (App-specific для Gmail) | `your_app_password` |

### Опциональные переменные

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `JWT_ISSUER` | Издатель JWT токенов | `CRUD_WebApi` |
| `JWT_AUDIENCE` | Аудитория JWT токенов | `CRUD_WebApiClient` |
| `JWT_EXPIRATION_MINUTES` | Время жизни токена (минуты) | `60` |
| `MAIL_HOST` | SMTP хост | `smtp.gmail.com` |
| `MAIL_PORT` | SMTP порт | `587` |
| `CORS_ALLOWED_ORIGINS` | Разрешенные CORS origins | `http://localhost:3000` |
| `DEFAULT_PERFORMER_EMAIL` | Email по умолчанию для исполнителей | `support@tasktrove.com` |
| `CONTACT_EMAIL` | Контактный email в уведомлениях | `hello@tasktrove.com` |
| `DEFAULT_CUSTOMER_NAME` | Имя заказчика по умолчанию | `Заказчик` |

Полный список см. в файле `.env.example`.

## Troubleshooting

### Проблема: "Access denied for user"
**Решение:**
- Проверьте правильность `DB_USERNAME` и `DB_PASSWORD` в `.env`
- Убедитесь, что база данных создана: `CREATE DATABASE tasktrove;`
- Проверьте права пользователя MySQL: `GRANT ALL PRIVILEGES ON tasktrove.* TO 'user'@'localhost';`

### Проблема: "JWT secret must be at least 256 bits"
**Решение:**
- Убедитесь, что `JWT_SECRET` содержит минимум 32 символа
- Используйте генератор случайных строк для создания безопасного секрета
- Пример: `openssl rand -base64 32`

### Проблема: "Failed to send email"
**Решение:**
- Проверьте `MAIL_USERNAME` и `MAIL_PASSWORD` в `.env`
- Для Gmail используйте App-specific password:
  1. Включите 2FA в Google аккаунте
  2. Перейдите в Security → App passwords
  3. Создайте новый app password
  4. Используйте его в `MAIL_PASSWORD`
- Проверьте настройки SMTP (host, port)
- Убедитесь, что firewall не блокирует порт 587

### Проблема: "CORS error"
**Решение:**
- Убедитесь, что `CORS_ALLOWED_ORIGINS` включает адрес вашего frontend
- Для нескольких origins используйте запятую: `http://localhost:3000,http://localhost:5173`
- Проверьте, что frontend делает запросы на правильный backend URL
- Очистите кэш браузера

### Проблема: "Environment variables not loaded"
**Решение:**
- Убедитесь, что файл `.env` находится в корне проекта
- Проверьте, что переменные экспортированы в вашей среде
- В IntelliJ IDEA: Run → Edit Configurations → Environment Variables
- В командной строке: `export $(cat .env | xargs)` (Linux/Mac)

### Проблема: "Port 8080 already in use"
**Решение:**
- Остановите другие приложения на порту 8080
- Или измените порт в `application.properties`: `server.port=8081`
- Найдите процесс: `lsof -i :8080` (Mac/Linux) или `netstat -ano | findstr :8080` (Windows)

### Проблема: "Frontend can't connect to backend"
**Решение:**
- Убедитесь, что backend запущен на `http://localhost:8080`
- Проверьте URL в `frontend/src/services/api.ts`
- Проверьте CORS конфигурацию
- Откройте Network tab в DevTools для диагностики

## Документация

### Основная документация
- **README.md** (этот файл) - Быстрый старт и основная информация
- **PROJECT_DESCRIPTION.md** - Подробное описание функциональности
- **ENVIRONMENT_VARIABLES.md** - Полный справочник переменных окружения

### API документация
- **API_RESPONSE_FORMAT.md** - Стандартный формат ответов API
- **API_CHANGES.md** - История изменений API

### Техническая документация
- **DATABASE_QUERY_OPTIMIZATION.md** - Оптимизация запросов к БД
- **.env.example** - Шаблон конфигурации

## Changelog

### Version 1.1.0 (Latest)
- ✅ Стандартизирован формат API ответов
- ✅ Добавлена транзакционность для критических операций
- ✅ Оптимизированы database queries
- ✅ Улучшена безопасность (environment variables)
- ✅ Очищен неиспользуемый код
- ✅ Стандартизировано логирование
- ✅ Обновлена документация

См. `API_CHANGES.md` для подробностей.

## Лицензия

[Укажите лицензию проекта]

## Контакты

[Укажите контактную информацию]
